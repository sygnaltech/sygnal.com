{
  "name": "sygnal-com",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://www.sygnal.com",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "esbuild ./src/*.ts --bundle --sourcemap --minify --outdir=dist --watch",
    "format": "prettier src/*.ts -w"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/learyjk/dev-template.git"
  },
  "author": "Michael Wells",
  "devDependencies": {
    "esbuild": "^0.15.14",
    "prettier": "^2.7.1",
    "serve": "^14.1.1",
    "typescript": "^4.9.3"
  }
}